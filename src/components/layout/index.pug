- const s = require('../styles/styles.json');
- const tokens = require('../tokens/build/tokens.json');

<!DOCTYPE html>
html(lang='en' dir='ltr')
	head
		include ../head/index.pug
	body(class=s.layout)
		block body
